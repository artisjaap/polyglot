jar {
    archiveBaseName = 'artisjaap-schedule'
    archiveVersion = '0.0.1-SNAPSHOT'
}

dependencies {
    compile project(':artisjaap-common')
    compile project(':artisjaap-migrate')
    
    compile 'org.springframework.boot:spring-boot-starter-freemarker'



}
