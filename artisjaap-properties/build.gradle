jar {
    archiveBaseName = 'artisjaap-properties'
    archiveVersion = '0.0.1-SNAPSHOT'
}

dependencies {
    compile project(':artisjaap-common')

    testCompile('org.springframework.boot:spring-boot-starter-test')

}
