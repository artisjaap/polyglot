import { NewTranslationForLessonAction } from './new-translation-for-lesson-action';

describe('NewTranslationForLessonAction', () => {
  it('should create an instance', () => {
    expect(new NewTranslationForLessonAction()).toBeTruthy();
  });
});
