import { LessonPracticeStatusResponse } from './lesson-practice-status-response';

describe('LessonPracticeStatusResponse', () => {
  it('should create an instance', () => {
    expect(new LessonPracticeStatusResponse()).toBeTruthy();
  });
});
